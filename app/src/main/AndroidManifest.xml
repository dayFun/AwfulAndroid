<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lighthouse.awfulandroid">

    <!--<uses-permission android:name="android.permission.SET_TIME_ZONE" />-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activities.application_entry.EntryActivity"
            android:label="@string/app_name"
            android:noHistory="true"
            android:launchMode="singleTop">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.instructions.InstructionsActivity" />
        <activity android:name=".activities.login.LoginActivity" />
        <activity android:name=".activities.interview_activities.InterviewActivities" />

    </application>

</manifest>


    <!--<activity--><!--android:name=".LoginActivity"--><!--android:label="@string/title_activity_name"--><!--android:theme="@style/AppTheme.NoActionBar"></activity>-->
