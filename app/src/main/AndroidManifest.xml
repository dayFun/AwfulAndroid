<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lighthouse.awfulandroid">

    <uses-permission android:name="android.permission.INTERNET"/>
    <!--<uses-permission android:name="android.permission.SET_TIME_ZONE" />-->

    <application
        android:name=".AwfulAndroidApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.activities.EntryActivity"
            android:label="@string/app_name"
            android:noHistory="true"
            android:launchMode="singleTop">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activities.instructions.InstructionsActivity" />
        <activity android:name=".ui.activities.LoginActivity" />
        <activity android:name=".ui.activities.interview.InterviewActivity" />
        <activity android:name=".ui.activities.lorem_ipsum.LoremIpsumActivity" />
        <activity android:name=".ui.activities.weather.WeatherActivity" />


        <service android:name=".services.CurrentConditionService"
            android:exported="false"/>
    </application>


</manifest>

